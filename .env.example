# Basic Auth 配置
# 支持多个账号，使用逗号分隔，每个账号以 username:password 表示
BASIC_AUTH_USERS=admin:adminadmin

# 会话签名密钥（至少 32 个字符），用于生成临时 cookie; openssl rand -base64 32
BASIC_AUTH_SESSION_SECRET=HkMmy5oDEB5dJEh189JNdp0h0A9x5YTxLECELBSuvzw=

# （可选）会话有效期，单位秒，默认 3600 秒
BASIC_AUTH_SESSION_TTL=3600

# （可选）自定义 cookie 名称
BASIC_AUTH_SESSION_COOKIE=burncloud_basic_session

# 其他已有配置示例
BURNCLOUD_API_KEY=
BURNCLOUD_BASE_URL=https://ai.burncloud.com
